# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Local machine config — the data root lives outside the repo
paths.yaml

# Private/local-only docs (generic pattern — reveals no intent in the ignore list itself)
*.local.md
.local/

# sim engine scratch (Replicator smoke renders)
sim/_smoke_out/

# Data / weights / artifacts (never commit large or licensed data)
data/raw/
data/processed/
datasets/
*.bin
*.pcd
*.npy
*.npz
*.pth
*.onnx
*.engine
mlflow.db
mlartifacts/
mlruns/
checkpoints/
logs/
wandb/

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Web viewer data (MVTec-derived, CC BY-NC-SA — generate locally, do not redistribute)
pointcloud-viewer/data/
.coverage

# external git-repo deps: checked out, never vendored (CLAUDE.md)
external/

# --- from sdlc-scaffold ---
# Beads / Dolt engine bits (issues.jsonl is tracked; the embedded engine is not)
# Coverage artifacts
# Data lives OUTSIDE the repo (paths.yaml points at it); never commit datasets/weights.
# External git-repo deps: checked out under external/, never vendored.
# MLflow local tracking store
# Python bytecode / build
# Run outputs / logs / local DBs
# Test / lint / type caches
# Virtualenv
# jscpd (node) — pulled transiently by npx for the duplication gate
*.pyc
.beads/*.db
.coverage*
.grimp_cache/
.import_linter_cache/
.pytest_cache/
.ruff_cache/
coverage.xml
data/
htmlcov/
node_modules/
runs/
