{
  "type": "npu_soc",
  "label": "SoC-integrated NPU",
  "op_support": {
    "conv_native": "native",
    "bank_lookup": "host_tail",
    "transformer_attention": "unsupported"
  },
  "note": "conv-native NPU sharing the SoC's LPDDR. Closer to the bank tail than a pure dataflow NPU — Gather and ArgMin are in the op-set, so a k=1 nearest-neighbour is nearly feasible on-device, but TopK is not, and the fp32 bank is not native; the tail is still a host residue.",
  "source": "research/deep_dives/2026-07-08_edge_accelerator_landscape.md (op-support) + 2026-07-15_edge_accelerator_datasheets.md (specs)",
  "instances": [
    {
      "name": "rknn_rk3588",
      "label": "Rockchip RK3588 RKNN NPU",
      "int8_tops": 6.0,
      "fp_supported": true,
      "int8_mandatory": false,
      "memory_model": "unified",
      "capacity_mib": null,
      "ext_memory": "shared system LPDDR4/4X/5 (+ ~956 KiB dedicated NPU SRAM per IP)",
      "sources": "S9 (6 TOPS int8, triple NPU), S10 (1 MiB SRAM / 956 KiB per IP + shared LPDDR)",
      "note": "6 TOPS int8 (triple NPU core). Gather/ArgMin on-device but TopK unsupported; k=1 argmin closer to feasible, fp32 bank still not native. Dedicated SRAM is tiny (~956 KiB); the working set lives in shared LPDDR."
    }
  ]
}
